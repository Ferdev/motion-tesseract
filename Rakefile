# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project'

Motion::Project::App.setup do |app|
  app.name = 'motion-tesseract'

  app.vendor_project( 'vendor/TesseractWrapper',
                      :xcode,
                      :headers_dir => 'TesseractWrapper' )
end
